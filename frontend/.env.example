# Frontend build-time config. On Vercel, set this in the project's
# Environment Variables so the app talks to your deployed backend.
# Local dev falls back to http://localhost:8000 when unset.
VITE_API_BASE_URL=https://lifeos-backend.onrender.com
