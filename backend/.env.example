# LifeOS backend reads Cognee Cloud credentials from the PROJECT ROOT .env.local
# (see ../.env.example). This file documents the same keys for reference.
#
# API-Base-URL=https://your-tenant.aws.cognee.ai
# API-KEY=your-cognee-api-key
# FRONTEND_ORIGIN=http://localhost:5173
